# Simple Auth UI + API App
The front end of the application is built in angular using components and ngrx for state management. Simple example of rbac on the UI.

The backend is developed using .NET with minimal APIs. 
